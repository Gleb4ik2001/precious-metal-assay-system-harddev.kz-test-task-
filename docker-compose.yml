services:
  api:
    build: .
    container_name: samples_api

    ports:
      - "8000:8000"

    env_file:
      - .env

    volumes:
      - ./samples.db:/app/samples.db

    restart: unless-stopped